{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Mirrors @docusaurus/tsconfig, minus the `baseUrl` option that TypeScript 7 removed. Once @docusaurus/tsconfig drops `baseUrl`, this can go back to a plain `extends`.",
  "compilerOptions": {
    "allowJs": true,
    "esModuleInterop": true,
    "jsx": "preserve",
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "moduleResolution": "bundler",
    "module": "esnext",
    "noEmit": true,
    "paths": {
      "@site/*": ["./*"]
    },
    "skipLibCheck": true
  }
}
